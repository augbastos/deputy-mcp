# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Environment — never commit real credentials
.env
.env.*
!.env.example

# OAuth token store — live access/refresh tokens, never commit
.deputy-mcp/
token.json
*.token.json

# Local scratch / demo scripts — not part of the package
scratchpad/

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
